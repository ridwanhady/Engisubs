package engisubs.gameobject.product.farmproduct.egg;

import org.junit.Assert;
import org.junit.Test;

public class TestEgg {
    // No test needed for pure Egg
    @Test
    public void autoTest(){
        Assert.assertTrue(true);
    }
}